# Independent check-in review evidence — 9 September 2026

Scope: local working tree at HEAD `46895fc2e9acfaba58753b8fa740f3804f934985`, including pre-existing uncommitted/ignored changes. Node v24.13.1; production frontend build Next 15.5.25. Application source was not edited. Review created only this document and the corrective plan. Disposable replica-set databases and synthetic browser fixtures only; no live DB, DNS, deployment or messages.

## Fresh baseline

From `D:/halla/halaa-checkin`:

- `npm run lint`: pass.
- `npm run design:check`: pass; note its implementation does not verify all provenance hashes.
- `npm --prefix contracts test`: 41/41 pass.
- `npm run build`: pass, 12 static pages. Next reports the scoped ESLint config does not include its plugin.
- From `api`: `node --test test/auth.test.js test/checkins.test.js test/config.test.js test/guests.test.js test/imports.test.js test/liveness.test.js`: 49/49 pass.
- From `web`: `node --test test/shell.test.js test/guests-unit.test.js test/gate-unit.test.js`: 24/24 pass. These include expression/contract tests rather than mounted-hook coverage.
- `npm run test:e2e`: 1/1 pass, exit 0, total process duration 93.99 seconds although the journey suite completed in approximately 4.35 seconds. It does not drive the React UI in a browser. Its un-cleared export deadline timers delay process completion after the test reports success.

The existing full browser and PDF evidence-generating suites were not rerun: their scripts overwrite historical evidence already modified in the working tree. The prior 61-API/36-web claims are not presented as fresh results here. No fresh physical-device, PDF visual, clean-checkout, Docker/capacity, backup/restore or production acceptance is claimed.

## Focused probes and observed outputs

### Git source coverage

Commands: `git ls-files halaa-checkin/api/src/modules/exports`; `git check-ignore -v halaa-checkin/api/src/modules/exports/*`; recursive local file count.

Observed: no tracked export-module source; ignore rule `halaa-checkin/.gitignore:26:exports/`; twelve local implementation files under that module. This explains why local tests/images can pass while a normal release checkout lacks required code.

### Queue race and private expiry

Used `setupTestDb()`/`stopReplSet()` from the existing test harness (fresh random mini-app test DB). Created one synthetic live event and a synthetic actor ID. Called `ExportsService.createExportJob(eventId,{kind:'report',locale:'en'},actor)` six times with `Promise.allSettled`, with worker stopped. Then set one private job snapshot to synthetic data, expired the job, ran cleanup against a new empty temporary directory, and re-read with `select('+snapshot')`.

Observed:

```json
{"probe":"concurrent per-admin queue limit 3","accepted":6,"rejected":0,"active":6}
{"probe":"expired export cleanup","state":"expired","snapshotRetained":true,"tokenRetained":true,"ttlIndexes":0}
```

No real guest names or tokens were used. Disposable database and empty temporary directory were removed by the probe's finally block.

### Duplicate CSV header

Called `parseAndValidateHeaders` with:

```csv
name,name,allowedCompanions,companionNames,reference
First,Second,0,,REF
```

Observed:

```json
{"probe":"duplicate CSV header","accepted":true,"parsedName":"Second"}
```

### Production-build browser probes

Started a dedicated `next start -p 3137` against the fresh local build. Used headless Playwright with the existing Chromium locator, synthetic API route fixtures and no writes to application data. These are targeted browser reproductions, not a claim of a fully integrated real-API journey.

1. Replay the actual session endpoint shape: `{data:{user:{role:'admin',...},csrfToken,expiresAt}}`, without the nonexistent top-level role. Navigate `/en/guests` with one synthetic event. Result: only Gate in workspace navigation, literal `roles.null` badge.
2. Supply a top-level admin role to isolate the separate empty-state defect from session normalization, return no events, and click “Create new event.” Result: zero dialogs. The button is unwired and parent layout suppresses the child creation UI.
3. Supply a synthetic live event and successful `getUserMedia` replacement returning `canvas.captureStream(10)`. Click Start camera. Result: video mounted, `srcObject` absent, returned track still live. The attachment occurs before conditional video mounting.

```json
{"probe":"actual-session","guestsNav":["Gate"],"nullRole":1}
{"probe":"empty-admin","dialogs":0}
{"probe":"camera","videoMounted":true,"streamAttached":false,"tracksLive":["live"]}
```

The first combined browser probe used an incorrect English button locator (“Create your first event”) and timed out after recording the session finding. It was rerun with the actual dictionary text (“Create new event”); the empty-state and camera outputs above are from the successful rerun. Browser contexts were closed in cleanup.

## Interpretation

Passing API concurrency/import tests are valuable and should be preserved. They do not prove frontend payload handling, real camera attachment, failure messages, bounded export queues, snapshot cleanup, or release source coverage. Each remaining code-confirmed finding, repair and required regression scenario is listed in [the finalization plan](../../implementation/hilton-guest-checkin/05-FINALIZATION-REVIEW-PLAN.md).
