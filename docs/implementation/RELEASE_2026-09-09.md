# Production release — September 9, 2026

## Released source

- Main change: b9e62887 — integrate Azure Maps, refine event flows, and add Hilton check-in app.
- Deployed application revision: 2dab9569245964f0c647e5dc0effb7e9631acb0d — complete Arabic event labels and canonical guest date formatting.
- Successful deployment: https://github.com/petersafwat11/halla/actions/runs/34293109642
- Both VPS API and web image tags match the deployed application revision; both containers healthy. Caddy reload and template restoration steps completed successfully.

## Configuration and verification

- CI website build explicitly used NEXT_PUBLIC_MAPS_PROVIDER=azure.
- VPS private configuration contains MAPS_PROVIDER=azure and the Azure server key. Existing configuration was backed up before modification; no keys committed or logged.
- Public HTTP checks: /health, /health/ready, /en, /ar all returned 200.
- Seeded host login succeeded. Authenticated Azure search returned five results; reverse lookup preserved the selected coordinates; rendering-session creation, attribution, and PNG tile retrieval succeeded (12,374-byte tile).
- Startup log review found no matching errors/warnings. Production initial invitation scheduler reports every-minute operation.
- No invitations were sent by deployment verification.
- Tests: backend 585, mobile 547, web 216, shared 213, standalone check-in 132 — all passed. Mobile and check-in lint passed. Website and check-in production builds passed; website build retains existing lint/metadata warnings.
- Runtime uploads remain on the persistent VPS volume and outside this source commit.

## Scope for tomorrow

- User handles UI/manual testing; fix reported issues and provide retest instructions.
- Hilton check-in progress is saved only, explicitly not published at the user's request. Separate deployment remains pending completion and testing.
- Mobile source is pushed. Installed native applications require a new binary for the added WebView dependency; no EAS/store release occurred.
- Full event lifecycle, reminder template approval/configuration, actual device checks and remaining manual QA are not certified complete.
- MA01 is saved for September 10 at 02:35 Riyadh. Its edited guest ending 6384 is outside the original QA recipient allowlist; restore an approved test recipient before testing a send.
